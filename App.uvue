<script setup lang="uts">
	// #ifdef APP-ANDROID || APP-HARMONY
	let firstBackTime = 0
	// #endif

	onLaunch(() => {
		console.log('App Launch')
	})

	onAppShow(() => {
		console.log('App Show')
	})

	onAppHide(() => {
		console.log('App Hide')
	})

	// #ifdef APP-ANDROID || APP-HARMONY
	onLastPageBackPress(() => {
		console.log('App LastPageBackPress')
		if (firstBackTime == 0) {
			uni.showToast({
				title: '再按一次退出应用',
				position: 'bottom',
			})
			firstBackTime = Date.now()
			setTimeout(() => {
				firstBackTime = 0
			}, 2000)
		} else if (Date.now() - firstBackTime < 2000) {
			firstBackTime = Date.now()
			uni.exit()
		}
	})

	onExit(() => {
		console.log('App Exit')
	})
	// #endif
</script>

<style>
	/* ============================================================
	   富记线上饭堂 · 简洁白 3.0 全局设计系统（唯一来源）
	   色板：纸白 #FFFFFF / 雾灰 #F6F6F4 / 墨黑 #141418
	         石墨 #86868B / 发丝线 #ECECEB / 炉火橙 #FF4A17
	   规则：炉火橙只用于 价格、结算行动、状态点徽章；
	         全站只用系统黑体，禁 serif / monospace / 渐变 / 大投影
	   g- 前缀类供所有页面直接引用，页面私有样式写在各页 <style>
	   ============================================================ */
	.uni-row {
		flex-direction: row;
	}

	.uni-column {
		flex-direction: column;
	}

	/* 小号间隔字（品牌副标 / 区块眉题） */
	.g-cap {
		font-size: 10px;
		font-weight: 700;
		letter-spacing: 0.24em;
		color: #86868B;
	}

	/* 次级说明文字 */
	.g-sub {
		font-size: 12px;
		color: #86868B;
	}

	/* 区块大标题 */
	.g-title {
		font-size: 20px;
		font-weight: 900;
		color: #141418;
	}

	/* 主按钮：墨黑胶囊 */
	.g-btn-primary {
		height: 48px;
		background-color: #141418;
		color: #FFFFFF;
		font-size: 15px;
		font-weight: 700;
		border: 0;
		border-radius: 999px;
		margin: 0;
		padding: 0;
	}

	.g-btn-primary[disabled] {
		background-color: #ECECEB;
		color: #B9B9BE;
	}

	/* 次按钮：白底发丝边胶囊 */
	.g-btn-ghost {
		height: 48px;
		background-color: #FFFFFF;
		color: #141418;
		font-size: 15px;
		font-weight: 600;
		border: 1px solid #ECECEB;
		border-radius: 999px;
		margin: 0;
		padding: 0;
	}

	/* 输入框：雾灰底无边框圆角 */
	.g-field-input {
		width: 100%;
		height: 46px;
		box-sizing: border-box;
		padding: 0 14px;
		background-color: #F6F6F4;
		border: 0;
		border-radius: 12px;
		font-size: 14px;
		color: #141418;
	}

	.g-ph {
		color: #B9B9BE;
	}

	/* 表单标签行 */
	.g-label {
		font-size: 12px;
		font-weight: 700;
		color: #141418;
	}

	.g-req {
		color: #FF4A17;
		font-size: 12px;
		margin-left: 3px;
	}

	/* 三态小标签：黑实 / 白描 / 雾灰 */
	.g-chip-solid {
		background-color: #141418;
		color: #FFFFFF;
		font-size: 11px;
		font-weight: 600;
		border-radius: 6px;
		padding: 5px 9px;
	}

	.g-chip-outline {
		background-color: #FFFFFF;
		color: #141418;
		font-size: 11px;
		font-weight: 600;
		border: 1px solid #ECECEB;
		border-radius: 6px;
		padding: 4px 8px;
	}

	.g-chip-dim {
		background-color: #F6F6F4;
		color: #86868B;
		font-size: 11px;
		font-weight: 600;
		border-radius: 6px;
		padding: 5px 9px;
	}

	/* 状态徽章：炉火橙实底（可取餐 / 进行中） */
	.g-chip-fire {
		background-color: #FF4A17;
		color: #FFFFFF;
		font-size: 11px;
		font-weight: 700;
		border-radius: 6px;
		padding: 4px 9px;
	}

	/* 价格：炉火橙加粗 */
	.g-price {
		color: #FF4A17;
		font-weight: 800;
	}

	/* 发丝分隔线 */
	.g-hairline {
		height: 1px;
		background-color: #ECECEB;
	}

	/* 全局轻提示 */
	.g-toast {
		position: fixed;
		left: 20%;
		right: 20%;
		bottom: 100px;
		background-color: rgba(20, 20, 24, 0.9);
		color: #FFFFFF;
		padding: 10px;
		border-radius: 10px;
		text-align: center;
		font-size: 13px;
	}
</style>
