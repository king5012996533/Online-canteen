# 富记线上饭堂 · 上线前检查清单

> 更新：2026-09-02　状态图例：✅ 完成　🔄 进行中　⏳ 待办
> **重大变更：不再做主体变更，改用新主体直接注册的新小程序（AppID `wx741ea5af08011b17`），旧个人号 `wxa9fac6425421ee50` 弃用。**

## 一、资质与账号（微信侧）

- ✅ 新号已注册：AppID `wx741ea5af08011b17`，主体：爸的执照
- ✅ 代码已切新凭据（manifest.json + cf-api/lib/config.js，2026-09-02）
- ⏳ **重新部署 cf-api**（AppSecret 在云函数里，不部署静默登录会失败）
- ⏳ 新号微信认证（按新号后台指引；若注册时已做法定代表人验证，确认认证状态即可）
- ⏳ **餐饮类目**：需《食品经营许可证》（凭执照到市监局/网上办理）→ 新号后台选类目
- ✅ **小程序备案（2026-09-04 通过 ✅）**：腾讯初审 → 工信部短信核验 → 管局审核全部完成，备案号已下发
  - 备案通过后：提审发布、微信支付关联均不再受阻塞
- ~~主体变更~~：已取消，不再走这条路

## 二、微信支付

- ✅ 商户号已注册（爸的执照名下，已审核通过）
- ✅ 支付代码已接入（2026-09-02）：V3 JSAPI 下单/拉起支付/主动查询/回调解密落库，饭盒支付屏「微信支付」真实扣款 +「模拟支付」双通道
- 🔄 商户平台 pay.weixin.qq.com → 产品中心 → AppID 账号管理 → **关联新 AppID `wx741ea5af08011b17`**（同主体，基本秒过；旧号的关联作废）
- 🔄 账户中心 → API 安全：**申请 API 证书**（下载 apiclient_key.pem 放入 `cf-api/lib/`，记下证书序列号）+ **设置 APIv3 密钥**；三件套（MCH_ID/序列号/APIv3 密钥）填入 `cf-api/lib/secrets.js` → 重新部署 cf-api → 真实小额测试单验证
- ⏳ 备齐归档：商户号 mchid、APIv3 密钥、商户证书序列号 + 商户私钥（apiclient_key.pem）
- ⏳ HTTPS 证书配好后：支付回调 notify_url 即可达（配好前用主动查询兜底，不影响测试）
- 备注：支付**不依赖**备案，可与备案并行推进

## 三、域名与后端（uniCloud）

- ✅ 域名 `xingtudesign.com` 已有 ICP 备案号（合法域名前提满足）
- ⏳ uniCloud 控制台给 `xk-api.xingtudesign.com` 配 HTTPS 证书 → 测 `https://` 能通
- ⏳ 上线时：新号后台「开发管理 → 开发设置 → 服务器域名」request 合法域名加 `https://xk-api.xingtudesign.com`
- ⏳ 代码改 https：`common/config.uts` 的 `API_BASE`（**单处**，前后端共用）

## 四、代码清理（提审前一次性做）

- ✅ 安全审计修复（2026-09-02）：订单接口鉴权收敛（查询/改状态只走口令校验的 admin）、机密出库（secrets.js 不入 Git）、后台口令限流、订单状态机校验、下单幂等键、token 30 天过期、菜单两阶段发布、生图 SSRF 防护、遗留 cf-admin/cf-menu/cf-order 从仓库删除
- ✅ 第二轮审计修复（2026-09-02）：profile 补 customer 导入（登录后崩溃）、首页补 toast 渲染节点、菜品分类动态收集、配送中 delivering 状态云端持久化、时间线 5 步、意见反馈接微信客服、退出登录真实化
- ⏳ **密钥轮换（需手动）**：① 公众平台「开发设置」重置 AppSecret；② 生图平台换 API key。新值填入 `cf-api/lib/secrets.js` 后重新部署 cf-api（后台口令已轮换为 `Fuji-Canteen-2026`，也在 secrets.js 里可随时改）
- ⏳ uniCloud 控制台删除云端残留的 cf-admin / cf-menu / cf-order 三个旧函数（代码里已删）
- ⏳ cf-api 云函数：`BYPASS_CUTOFF` 改回 false（lib/config.js）
- ⏳ index.uvue：`testBypass` 改回 false
- ⏳ manifest.json：`urlCheck` 改回 true（HTTPS 配好之后）
- ✅ 厨房页网络异常加固：静默轮询 + 断连横条 + 12 秒快速重试
- ✅ 蓝牙打印全链路：扫描 / 连接 / GBK 中文出票正常

## 五、提审与发布

- ⏳ 提审前确认三件套：备案已完成、类目已选、用户隐私保护指引已配置（按实际用到的权限勾：位置信息、蓝牙等）
- ⏳ 体验版完整走一遍：点单 → 支付 → 厨房接单 → 蓝牙打印 → 配送 → 送达
- ⏳ 提交审核 → 发布上线

## 已知风险 / 备忘

- 餐饮类目审核要求证照与服务内容一致（现炒 + 配送/自取模式写清）
- 新号是全新 openid 体系：users 表里旧测试记录（openid 属旧 AppID）**提审前清空**；orders 测试订单一并清
- 旧个人号处置：闲置或注销，注意后台别再被人改动
- 新号后台要重配：用户隐私保护指引（openid/手机号/蓝牙）、服务器域名（等 HTTPS）、体验版成员
- 旧号残留清理（可选）：uniCloud 控制台旧 URL 化路径
- 「网络异常」偶发已加固，上线后留意 uniCloud 按量计费账单
